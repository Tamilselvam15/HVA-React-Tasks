/* eslint-disable react/prop-types */

const Font_Display = ({size}) => {
  return (
      <div className="m-auto " >
          <h1 style={{ fontSize: `${size}px` }} className="border border-solid font-serif font-bold text-red-400" >hello </h1>
    </div>
  )
}

export default Font_Display